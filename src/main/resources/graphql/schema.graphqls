type User{
    id: ID!
    name:String
    email:String
}

type Post{
    id: ID!
    content:String
    author:User!
}

type Query{
    userList:[User!]!
    user(id:ID!): User
    postList:[Post!]!
    post(id:ID!): Post
}